# Requirements for model_v1 (fastai-free inference)
# This environment is for testing the ported model without fastai dependencies

# Core PyTorch stack
torch>=2.0.0
torchaudio>=2.0.0
torchvision>=0.15.0

# Audio processing (replaces fastai_audio)
librosa>=0.10.0
soundfile>=0.12.0
scipy>=1.10.0  # For resample_poly (matches fastai_audio's resampling)

# Data processing
numpy>=1.24.0
pandas>=2.0.0

# Visualization (for test spectrograms)
matplotlib>=3.7.0

# Configuration
pyyaml>=6.0

# Audio file handling
pydub>=0.25.0

# Testing
pytest>=7.0.0
pytest-cov>=4.0.0

# Optional: HuggingFace (for future model upload)
# transformers>=4.30.0
# huggingface-hub>=0.16.0
